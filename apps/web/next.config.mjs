/** @type {import('next').NextConfig} */
const nextConfig = {
  // Backend proxy is implemented as an App Router route handler:
  // `src/app/api/v1/[...path]/route.ts`
};

export default nextConfig;
